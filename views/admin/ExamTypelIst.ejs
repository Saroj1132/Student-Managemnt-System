<%- include('.../../../paritals/header') %>
<div class="containers">
  <a href="/Course/Exam_Type" class="btn btn-primary">Add New Exam Page</a>

  <br>
  <br>
            <div class="row">
                <h1>Exam Type List</h1>
                <table class="table table-striped table-primary">
                    <thead>
                        <tr>
                            <th>Exam_Type</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%row.forEach(function(p){ %>
                        <tr>
                            <td><%=p.Exam_Type%></td>
                            <td>
                            <a class="btn btn-primary btn-xs" href="/Course/examType/edit/<%=p._id%>"><i class="glyphicon glyphicon-edit"></i></a>
                            <a class="btn btn-danger btn-xs" href="/Course/examType/delete/<%=p._id%>"><i class="glyphicon glyphicon-trash"></i></a>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
